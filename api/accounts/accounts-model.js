const db = require("../../data/db-config")
const getAll = () => {
  return "getAll wired"
}

const getById = id => {
  return " wired"
}

const create = account => {
  return " wired"
}

const updateById = (id, account) => {
  return " wired"
}

const deleteById = id => {
  return " wired"
}

module.exports = {
  getAll,
  getById,
  create,
  updateById,
  deleteById,
}
